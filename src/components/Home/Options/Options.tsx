import React from "react";
import styles from "./Options.module.scss";

const Options: React.FC = () => {
	return (
		<div className={styles.optionsContainer}>
			<h3 className={styles.title}>Выберите опцию</h3>
		</div>
	);
};

export default Options;
