import React from "react";
import styles from "./ContactElements.module.scss";

const ContactElements: React.FC = () => (
	<div className={styles.container}>
		<h2>ContactElements</h2>
	</div>
);

export default ContactElements;
